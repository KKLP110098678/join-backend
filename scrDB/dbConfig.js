const firebaseConfig = {
  databaseURL:
    "https://join-1325-default-rtdb.europe-west1.firebasedatabase.app/",
};


const app = firebase.initializeApp(firebaseConfig);

const db = firebase.database();
